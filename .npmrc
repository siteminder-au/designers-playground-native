legacy-peer-deps=true

# Pin to public npm so installs do not rewrite lockfile resolved URLs to a
# private registry (CI/Heroku npm ci has no Artifactory credentials -> E401).
registry=https://registry.npmjs.org/
