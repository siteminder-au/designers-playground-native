// Hides every Tap to Pay entry point (Reservations banner/chip/hero,
// Payment details row/button/banner/badge, Take Payment modal variants)
// without removing the underlying feature or its demo-flag variants.
// Flip back to true to restore them.
export const SHOW_TAP_TO_PAY_ENTRY_POINTS = false;
